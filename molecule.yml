---
ansible:
  playbook: tests/playbook.yml
driver:
  name: docker
verifier:
  name: testinfra
docker:
  containers:
    - name: merge-test
      image: ubuntu
      image_version: xenial
      ansible_groups:
        - group1
    - name: merge-test
      image: centos
      image_version: 7
      ansible_groups:
        - group2

